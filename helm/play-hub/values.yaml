frontend:
  image:
    repository: ghcr.io/kkamji98/play-hub-frontend
    tag: d4d3b3f20473c1cb59cd8b8282d711f818e9461f # frontend-tag
    pullPolicy: IfNotPresent
  containerPort: 8080
  replicas: 1
  service:
    port: 80
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

backend:
  image:
    repository: ghcr.io/kkamji98/play-hub-backend
    tag: d4d3b3f20473c1cb59cd8b8282d711f818e9461f # backend-tag
    pullPolicy: IfNotPresent
  replicas: 1
  service:
    port: 8080
  resources:
    requests:
      cpu: 200m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

ingress:
  enabled: true
  className: nginx
  host: play-hub.kkamji.net
  tls:
    enabled: true
    clusterIssuer: letsencrypt-prod
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/upstream-hash-by: "$remote_addr"
